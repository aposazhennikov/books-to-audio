# Desktop install profile.
#
# Prefer the cross-platform installer:
#   python install.py
#
# This file is kept for users and CI systems that expect requirements.txt.
# It installs the project in editable mode with GUI, OCR, LLM, and audio helpers.
-e .[desktop]
