# Dependency Changes

- Add new dependencies only when they provide clear value.
- Update lock files together with dependency manifest files.
- Explain why each new dependency is needed in the commit message or PR description.
