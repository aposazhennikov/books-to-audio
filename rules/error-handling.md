# Error Handling

- User-facing errors should be clear, actionable, and specific enough to guide the next step.
- Avoid showing raw tracebacks in normal UI or CLI flows when a helpful message can be shown instead.
