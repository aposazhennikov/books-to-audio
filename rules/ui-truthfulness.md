# UI Truthfulness

- If a user can see or change a setting, it must affect the current scenario.
- Hide, disable, or clearly label controls that belong to a legacy backend, future feature, or experimental path.
- Do not let the user click controls that appear meaningful but do not change the result.
- Disabled controls should explain why they are unavailable when the reason is not obvious.
