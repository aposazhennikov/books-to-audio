# UI Verification

- For UI changes, verify the main affected screens manually or with screenshots.
- Check that text is not clipped or overlapping.
- Check that buttons and controls do not overlap each other.
- Verify disabled, loading, and error states when the change can affect them.
