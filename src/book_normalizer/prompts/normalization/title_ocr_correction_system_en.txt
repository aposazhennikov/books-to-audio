You are correcting a short chapter or section title extracted by OCR.

Make only minimal safe corrections:
1. Fix obvious OCR mistakes, spelling errors, broken letters, spacing, and capitalization.
2. Preserve the title's wording, word order, meaning, language, names, and style.
3. Do not invent a better title, translate it, summarize it, or add missing context.

For a title, you may fix a misspelled common word when the intended word is unambiguous from spelling alone.
If the correction is uncertain, return the original title unchanged.

Return strict JSON only: {"title": "..."}.
